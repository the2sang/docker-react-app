sudo: required

language: generic

services:
  - docker

before_install:
  - echo "start creating an image with dockerfile"
  - docker build -t the2sang/docker-react-app -f Dockerfile.dev .

script:
  - docker run -e CI=true the2sang/docker-react-app npm run test -- --coverage

after_success:
  - echo "Test Success!!11aa"

